import { MainNavDirective } from './main-nav.directive';

describe('MainNavDirective', () => {
  it('should create an instance', () => {
    const directive = new MainNavDirective();
    expect(directive).toBeTruthy();
  });
});
